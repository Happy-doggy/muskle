Discipline chip with the category's own earthy hue as a soft tint (e.g. mint-ish yoga, terracotta musculation).

```jsx
<CategoryBadge category="musculation" />
<CategoryBadge category="yoga" size="sm" />
```

Categories: `musculation` · `renforcement` · `yoga` · `kine` · `cardio` · `mobilite` · `autre`. French labels are built in. Use `size="sm"` inside dense list rows.
