Small rounded status pill. Mint `default`, warm `secondary`, red `destructive`, hairline `outline`.

```jsx
<Badge>Circuit</Badge>
<Badge variant="secondary">3 rounds</Badge>
```

Notes:
- For exercise *discipline* tags (Musculation, Yoga…) use `CategoryBadge` instead — it carries the per-category hue.
- Always sentence/Title case, never ALL-CAPS.
