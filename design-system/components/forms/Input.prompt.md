White text field + label for Muskle's editor forms. Mint focus ring, 10px corners.

```jsx
<div>
  <Label htmlFor="name">Nom de l'exercice</Label>
  <Input id="name" placeholder="Squat bulgare" />
</div>
```

Notes:
- Always pair `Label` + `Input` with matching `htmlFor`/`id`.
- French placeholders, sentence case.
